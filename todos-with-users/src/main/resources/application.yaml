spring:
  sql:
    init:
      mode: always
  docker:
    compose:
      file: compose.yaml
#logging:
#  level:
#    org.springframework.security: TRACE
server:
  servlet:
    session:
      persistent: true